#include <bits/stdc++.h>
#define rep(i , a , b) for(int i = a; i <= b; ++i)
#define rrep(i , a , b) for(int i = a; i >= b; --i)
using namespace std;
typedef long long ll;
const ll MOD = 1e9 + 7;
int N , M;
vector<string> T;
// TODO
int main(){
    cin >> N >> M;
    rep(i , 0 , N - 1){
        string t;
        cin >> t;
        T.push_back(t);
    }

}