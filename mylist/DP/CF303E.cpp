#include <bits/stdc++.h>
using namespace std;

const int maxn = 85;
int L[maxn] , R[maxn];
int N;
double ans[maxn][maxn];

int main(){
    cin >> N;
    for(int i = 1; i <= N; ++i){
        cin >> L[i] >> R[i];
    }
    
}
